function extract(){

    const user = {name: "Bob", age: 30, city: "USA"};

    console.log(`Hello my name is ${user.name}`, `and I am ${user.age}`,`years old`); 
    //using $user.name and $user.age to extract the values from the object from user 
}

extract();
